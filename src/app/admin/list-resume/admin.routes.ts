import { Routes } from "@angular/router";
import { ListResumeComponent } from "./list-resume.component";

export const routes: Routes = [
    { path: 'list-resumes', component: ListResumeComponent },
  ];